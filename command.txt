uvicorn backend.main:app --reload

//run using cloudflare or tunnel for mobile
1. uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
2. cloudflared tunnel --url http://localhost:8000

or

2. lt --port 8000 --subdomain levidyo
